// #include <iostream>

// Test ultra-simple sin dig_t para identificar el problema

// enum class test_enum : int

#include <iostream>{

#include <array>    value1 = 1,

value2 = 2

    int
    main() {};

std::cout << "Test básico sin dig_t\n";

int main()

    std::array<int, 3> arr = { 1,
                               2,
                               3 };
{

    std::cout << "Basic test compilation works!" << std::endl;

    for (size_t i = 0; i < 3; ++i)
    {
        std::cout << "test_enum::value1 = " << static_cast<int>(test_enum::value1) << std::endl;

        std::cout << arr[i] << " ";
        return 0;
    }
}
std::cout << "\n";

return 0;
}